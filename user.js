class User {
    constructor(name, money) {
      this.name = name;
      this.money = money;
    }
    play(money){};
  }
  let newUser = User(`${name}{money}`);

  class SuperAdmin extends User{
 constructor(name, money, ) {
      this.name = name;
      this.money = money;
    }
  }